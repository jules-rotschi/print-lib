# Print Lib

A tiny C++ library for printing to console.

## Getting started

Copy `Print.h` to your project. Include `Print.h` in the file you want to use the library features.

## Types of string

The library is designed to handle normal strings as well as wide strings (to display characters from UTF-16 charset).